name = "pec-config"
main = "worker.js"
compatibility_date = "2024-11-01"

# Created by `wrangler kv namespace create CONFIG` — paste the printed id here.
kv_namespaces = [
  { binding = "CONFIG", id = "7b417cbe729f43ceaded244d23b0a5bd" }
]

# PASSKEY is a secret, not set here. Add it with:
#   npx wrangler secret put PASSKEY
